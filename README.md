# Library Core Utils

This library objective is to decouple some utilitary classes related to XMK, security or pathes to allow plugins or libs to use those classes without the need to depend on lutece-core.
